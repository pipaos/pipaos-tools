#!/bin/bash
#
# debian postinst script
#

pipaos_network=/boot/interfaces.txt

case "$1" in

    configure)
        if [ ! -f $pipaos_network ]; then
            cp /usr/share/pipaos-tools/interfaces-pipaos.txt $pipaos_network
        fi

        ln -sfv "$pipaos_network" /etc/network/interfaces
        ;;
esac

#DEBHELPER#

exit 0
